import { ReviewTypePipe } from './review-type.pipe';

describe('ReviewTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReviewTypePipe();
    expect(pipe).toBeTruthy();
  });
});
